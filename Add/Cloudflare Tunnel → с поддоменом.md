# Cloudflare Tunnel → локальный сервер (macOS)

Мы настраиваем **Cloudflare Tunnel** от Cloudflare к твоему локальному серверу.  
Один раз. Потом он просто работает.

---

## Поддомен и основной сайт

> [!info]
> Туннель привязываешь к **поддомену** (например `testing.neetrino.com` или `dev.neetrino.com`). Основной сайт `neetrino.com` при этом остаётся на своём сервере — ничего не переезжает и не ломается.

Cloudflare разводит трафик:
- `neetrino.com` → твой прод-сервер (как и раньше)
- `testing.neetrino.com` → туннель → твой `localhost:3000`

Один домен, разные маршруты.

---

## Шаг 1. Создай бесплатный аккаунт Cloudflare

👉 [dash.cloudflare.com](https://dash.cloudflare.com/)

Если есть домен — отлично. Если нет — можно и без своего домена (Cloudflare даст технический).

---

## Шаг 2. Установи cloudflared

Установка:

```bash
brew install cloudflare/cloudflare/cloudflared
```

Проверка:

```bash
cloudflared version
```

---

## Шаг 3. Авторизуй компьютер в Cloudflare

```bash
cloudflared tunnel login
```

Откроется браузер → разрешаешь доступ → готово.

(Это не “вход навсегда”, это привязка устройства)

---

## Шаг 4. Создай туннель

```bash
cloudflared tunnel create local-server
```

В выводе будет: `Created tunnel with ID: ...` и путь к файлу в `~/.cloudflared/`.

---

## Шаг 5. Свяжи туннель с поддоменом

Если домен в Cloudflare:

```bash
cloudflared tunnel route dns local-server dev.neetrino.com
```

Или тестовый поддомен:

```bash
cloudflared tunnel route dns local-server testing.neetrino.com
```

После этого выбранный поддомен ведёт в туннель. Если домена нет — переходи к шагу 7, Cloudflare покажет временный URL.

---

## Шаг 6. Конфиг: привязка к localhost:3000

Создай или открой конфиг:

```bash
nano ~/.cloudflared/config.yml
```

Вставь (путь к `.json` — из вывода шага 4):

```yaml
tunnel: local-server
credentials-file: /Users/user/.cloudflared/ecf951db-c213-42c2-91aa-18ebc2f7b667.json

ingress:
  - hostname: dev.neetrino.com
    service: http://localhost:3000
  - service: http_status:404
```

> [!tip] Копирование
> У блоков кода в Obsidian справа есть кнопка копирования — нажми её, чтобы вставить команду в терминал без лишних символов.

---

## Шаг 7. Запуск туннеля

```bash
cloudflared tunnel run local-server
```

Готово. Твой `localhost:3000` доступен из интернета по поддомену, через HTTPS, без открытых портов.

---

## Итог

| Фича            | Есть |
|-----------------|------|
| Постоянный URL  | ✅   |
| Бесплатно       | ✅   |
| Без NAT/портов  | ✅   |
| HTTPS           | ✅   |

---

## Когда пригодится

- Webhook'и (Bitrix, Stripe, Telegram)
- Демо клиентам
- Тесты Next.js / Nest.js
- Мобилки, API

---

### Кратко

ngrok = «покажи на 10 минут». Cloudflare Tunnel = постоянный URL. Дальше: автозапуск на Mac, несколько портов, защита паролем.